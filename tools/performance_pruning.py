class PerformancePruning:
    @staticmethod
    def prune(codes, k):
        # Placeholder for actual logic to prune codes based on performance
        return sorted(codes, key=lambda x: x[1], reverse=True)[:k]
